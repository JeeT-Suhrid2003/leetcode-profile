{
	"version": 2,
	"builds": [
		{ "src": "package.json", "use": "@vercel/static-build" }
	],
	"routes": [
		{ "src": "/data/stats.json", "headers": { "cache-control": "s-maxage=300, stale-while-revalidate=600" }, "dest": "/data/stats.json" }
	]
}
